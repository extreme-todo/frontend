name: CI to develop

on:
  pull_request:
    branches: ["develop"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: package install
        run: npm ci
      - name: test
        run: npm run test
        env:
          CI: true
